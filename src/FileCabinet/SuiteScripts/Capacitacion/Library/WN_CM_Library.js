/**
* @NApiVersion 2.1
*/
define(['N/log'], (log) => {
    const getSuma = (operand1, operand2) => {
        log.debug('Debug', 'Entry');
        //return operand1 + operand2;
    }

    return {
        getSuma
    }
});